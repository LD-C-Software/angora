import { describe, expect, it } from 'vitest'

// TODO: replace with real tests. This exists only to give the CI test
// step something to run before real coverage is written.
describe('placeholder', () => {
  it('CI test wiring works', () => {
    expect(true).toBe(true)
  })
})
